{
  "name": "alola_credpal",
  "version": "1.0.0",
  "description": "Automation of credpal homepage and affiliates marketing page using cypress as an automation framework and javascript as a programming language while adopting page object model and bdd with cucumber",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bdd",
    "pom",
    "cucumber",
    "javascript"
  ],
  "author": "David Alola",
  "license": "ISC",
  "html": {
    "enabled": true
  },
  "stepDefinitions": [
    "cypress/e2e/[filepath]/**/*.{js,ts}",
    "cypress/e2e/[filepath].{js,ts}",
    "cypress/support/step_definitions/*.{js,ts}"
  ],
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "latest",
    "@bahmutov/cypress-esbuild-preprocessor": "latest",
    "cypress": "latest"
  }
}
